<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardBackgroundColor="#1AFFFFFF"
    app:cardCornerRadius="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:gravity="center_horizontal">

        <!-- Sigla e Nome -->
        <TextView
            android:id="@+id/poluente_sigla"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="PM2.5"
            android:textColor="#B3FFFFFF"
            android:textSize="12sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/poluente_nome_completo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Partículas Finas"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:textAlignment="center"
            android:layout_marginTop="2dp"
            android:maxLines="2"
            android:ellipsize="end"/>

        <!-- Espaçador -->
        <View
            android:layout_width="match_parent"
            android:layout_height="8dp"/>

        <!-- Valor com melhor formatação -->
        <TextView
            android:id="@+id/poluente_valor"
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:text="26.12 pg/m³"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textAlignment="center"
            android:gravity="center"
            android:maxLines="1"
            android:autoSizeTextType="uniform"
            android:autoSizeMinTextSize="10sp"
            android:autoSizeMaxTextSize="20sp"
            android:autoSizeStepGranularity="1sp"
            android:includeFontPadding="false"/>

        <!-- Status -->
        <TextView
            android:id="@+id/poluente_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Bom"
            android:textColor="#4CAF50"
            android:textSize="12sp"
            android:textStyle="bold"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>