<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões para Internet e Localização -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />


    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />




    <application
        android:name=".AsthmaSpaceApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoAsthmaSpace"
        android:usesCleartextTraffic="true">

        <!-- Qualidade do ar -->
        <receiver
            android:name=".Data.worker.QualidadeArReceiver"
            android:exported="false" />

        <!-- Lembretes -->
        <receiver
            android:name=".Data.worker.LembreteReceiver"
            android:exported="false" />

        <!-- Reboot -->
        <receiver
            android:name=".Data.worker.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>




        <!-- Activity de Login é a tela de início -->
        <!--        android:name=".AppStart"-->
        <!--Vai ser a activity principal, porem agora para testes nao -->
        <activity
            android:name=".Presentation.ActivityView.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ProjetoAsthmaSpace.SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras Activities do fluxo de cadastro -->
        <activity
            android:name=".Presentation.ActivityView.CadastrarActivity"
            android:exported="true" />
        <activity
            android:name=".Presentation.ActivityView.InfAdicionaisActivity"
            android:exported="true" />

        <activity android:name=".Presentation.ActivityView.InformacoesPessoaisActivity" android:exported="false" />


        <activity
            android:name=".Presentation.ActivityView.EsqueceuSenhaActivity"
            android:windowSoftInputMode="adjustResize" />



        <!-- Activity Principal (com os fragmentos) -->
        <activity
            android:name=".Presentation.ActivityView.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".Presentation.ActivityView.AjudaActivity" />
<!--        <activity android:name=".ActivityView.Perfil" />-->
        <activity android:name=".Presentation.ActivityView.NotificacoesActivity" />
        <activity android:name=".Presentation.ActivityView.ConfiguracoesActivity" />
        <activity android:name=".Presentation.ActivityView.AlterarSenhaActivity" />



        <!-- FileProvider para compartilhar arquivos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>